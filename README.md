# Hlix Homebrew Tap

Install the public Hlix CLI release with Homebrew:

```bash
brew tap hlix-ai/tap
brew install hlix
```

The formula is updated automatically after a verified `@hlix/cli` release.
